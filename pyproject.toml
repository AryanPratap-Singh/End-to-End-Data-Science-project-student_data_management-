[project]
name = "student-dashboard"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Aryan Pratap Singh", email = "aryansingh87615@gmail.com" }
]
requires-python = ">=3.13"
dependencies = []

[project.scripts]
student-dashboard = "student_dashboard:main"

[build-system]
requires = ["uv_build>=0.12.9,<0.13.0"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "ipykernel>=7.3.0",
    "pandas",
    "numpy",
    "seaborn",
    "matplotlib",
    "scikit-learn",
]
